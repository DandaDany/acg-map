# 工作指派單（for Claude Code）

開單：2026/06/27　技術主管：Claude（網頁維護員）
這是主清單，三件事**照順序**做；細節各看對應文件。全程繁體中文（台灣用語）。

---

## 優先順序與內容

### P0 — 文化部場館大量消失（先修，資料遺失）
- 文件：`BUG_文化部場館消失_for_ClaudeCode.md`
- 重點：06/25→06/27 場館 224→81、活動 449→215，集中在 moc(文化部)層(約140→12)，非到期。
- 先做的決定性一步：Mac 端 `curl` 文化部 API 數筆數，判斷是來源變少還是我們處理掉了。
- 為什麼第一：這是資料正確性問題，且 logo/分類都建立在完整資料上，資料不齊修後面沒意義。

### P1 — Logo 根因修復（DDG favicon 備援）
- 文件：見 `Agent交流工作日誌.txt` 2026/06/27 #2 條目。
- 重點：venue_logo() 移除 DuckDuckGo/favicon 代理當備援；改 logo_map本地檔 → FB頭貼 → 官網 apple-touch-icon/og:image → 否則留空。
  collect_logos 拒收 <2KB 點陣 favicon（svg 不受限）；collect_fb_logos 拒收 1876B 佔位圖與 profile.php?id=/p/ 頁。
- 註：我已先把當前地圖 59 個 DDG 錯圖退回預設（interim）；不改 venue_logo() 的話，重跑會全部回來。

### P2 — 分類重構（兩軸：主題 × 形式）
- 文件：`分類重構規格_for_ClaudeCode.md`
- 重點：軸A主題(ACG/藝術設計/其他文化)＋軸B形式(展覽/快閃店/主題餐廳/體驗活動/其他)；
  判斷兩層（Excel一定ACG；其他來源與Excel重疊也算ACG，沒重疊才關鍵字）；前端拆成兩個交叉篩選。

> 三件可在**同一輪**處理：P0 先查修讓資料回齊 → P1 改 logo 取得邏輯 → P2 加分類欄位與前端 → 最後**一次** update_all.py 重跑驗收。

---

## 工作守則（務必遵守，這是本專案的硬規則）
1. **每個改動可追溯到需求**：改了什麼、對應哪張單／哪條需求，寫進 `Agent交流工作日誌.txt`。
2. **AI 自行決定的另外列**：關鍵字表、軸名稱、分類門檻等你自己定的，獨立列出讓使用者檢查，別混進「已完成」。
3. **不可逆操作先停下問人**：刪資料、改檔案結構、覆蓋原始 Excel/原始資料、推上線前——先問，不要自行執行。
4. **「已完成」≠完成**：每項交付標可信度 ✅已驗證 / ⚠️部分驗證 / ❌未驗證，並寫出你用什麼方式驗證。
5. **改前備份、另存新檔**：venues.json、refresh_venues.py、Excel 動之前先備份；不直接破壞原始資料。
6. **不要誤傷其他層**：修 moc 時不得讓 manual(ACG)/official/caco/cayenne 變少或變動。
7. **不亂補無來源資料**：日期/地址/授權商查不到就留空標「需人工確認」，不要推測。

---

## 整批驗收（Definition of Done）
重跑 update_all.py 後，一次產出對照給使用者：
- **資料量**：場館數、各 src 分布、活動數，對照 06/25(224/449)，差異需用「真正到期」解釋；moc 層回到合理數。
- **Logo**：列出仍無 logo 的場館；確認地圖上沒有 favicon 代理圖／<2KB 破圖／1876B FB 假圖。
- **分類**：產「舊7類 → 新軸A＋軸B」對照與分布，列出所有「需人工確認」；前端兩個篩選交叉可運作。
- **root cause**：P0 的真正原因寫清楚，並（若為 soft-fail）加上「抓到筆數過低就中止、保留上一版」的防呆。
- 全部結論回寫 `Agent交流工作日誌.txt`。
