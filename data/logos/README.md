# data/logos

logo 對照表與 fallback metadata。

重要檔案：
- `venue_logos.json`：場館名稱關鍵字對官方網域。
- `logo_map.json`：官方網域對 `public/logos/...` 圖片路徑。
- `fb_pages.json`、`fb_logos.json`：Facebook fallback logo。
- `artemperor_logos.json`：非池中 fallback logo。

實體圖片放在 `public/logos/`，這裡主要放對照資料。
