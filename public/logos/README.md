# public/logos

前端公開 logo 圖片。

內容：
- 根目錄圖片：官方網站抓到的 logo。
- `facebook/`：Facebook fallback logo。
- `artemperor/`：非池中 fallback logo。
- `_thumbs/`：地圖 marker 使用的小尺寸 WebP 縮圖。

`public/venues.json` 裡的 logo 欄位會使用 `logos/...` 相對路徑。
