# archive/backups/logo

放 logo 相關備份，例如舊版 `logo_map.json`、`fb_logos.json`、`venue_logos.json` 或當時的 `venues.json` 備份。

用途：
- logo 對照檔損壞時還原。
- 比對錯圖或缺圖問題。

最新 logo metadata 在 `data/logos/`，前端圖片在 `public/logos/`。
